<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Information Form</title>
  <link rel="stylesheet" href="/chatstyles.css"> <!-- You can link to a CSS file if you need styling -->
</head>
<body>
  <header></header>
  <nav class="navbar">
      <div class="logo">
          <a href="/">Turtle Shelter Project</a>
      </div>
      <ul class="nav-links">
          <li><a href="#donate">Donate</a></li>
          <li><a href="/addVolunteer">Volunteer</a></li>
          <li><a href="/login">Sign In</a></li>
          <li><a href="/createRequest">Request an Event</a></li>
      </ul>
  </nav>
</header>
<div class="form-container"> 
  <form action="/editVolunteer/<%= volunteer.vol_id %>" method="POST">
    <h1>Edit Volunteer</h1>
    <div>
      <label for="firstName">First Name:</label>
      <input type="text" id="firstName" name="firstName" required value="<%= volunteer.vol_first_name %>">
    </div>
    
    <div>
      <label for="lastName">Last Name:</label>
      <input type="text" id="lastName" name="lastName" required value="<%= volunteer.vol_last_name %>">
    </div>

    <div>
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required value="<%= volunteer.vol_email %>">
    </div>

    <div>
      <label for="phone">Phone Number:</label>
      <input type="tel" id="phone" name="phone" pattern="^\+?[1-9]\d{1,14}$" required value="<%= volunteer.vol_phone %>">
    </div>

    <div>
      <label for="password">Enter a Password:</label>
      <input type="password" id="password" name="password" required value="<%= volunteer.password %>">
    </div>

    <div>
      <label for="address1">Street Address 1:</label>
      <input type="text" id="address1" name="address1" required value="<%= volunteer.vol_street_1 %>">
    </div>

    <div>
      <label for="address2">Street Address 2:</label>
      <input type="text" id="address2" name="address2" value="<%= volunteer.vol_street_2 %>">
    </div>

    <div>
      <label for="city">City:</label>
      <input type="text" id="city" name="city" required value="<%= volunteer.vol_city %>">
    </div>

    <div>
      <label for="state">State:</label>
      <input type="text" id="state" name="state" maxlength="2" required value="<%= volunteer.vol_state %>">
    </div>

    <div>
      <label for="zip">ZIP Code:</label>
      <input type="text" id="zip" name="zip" pattern="^\d{5}(-\d{4})?$" required value="<%= volunteer.vol_zip %>">
    </div>

    <div>
      <label for="source">How did you hear about us?</label>
      <select name="source" id="source">
        <% source.forEach(item => { %>
            <option value="<%= item.source_id %>" <%= volunteer.source_id === item.source_id ? 'selected' : '' %>>
                <%= item.source_type %>
            </option>
        <% }) %>
    </select>    
    </div>

    <div>
        <label for="role">Role:</label>
        <select name="role" id="role">
            <% role.forEach(item => { %>
                <option value="<%= item.role_id %>" <%= volunteer.role_id === item.role_id ? 'selected' : '' %>>
                    <%= item.role_name %></option>
            <% }) %>
        </select>
      </div>

    <div>
        <label for="sewLevel">Sewing Level:</label>
        <select name="sewLevel" id="sewLevel">
            <% proficiency.forEach(item => { %>
                <option value="<%= item.level_id %>" <%= volunteer.vol_sew_level_id === item.level_id ? 'selected' : '' %>>
                    <%= item.level %></option>
            <% }) %>
        </select>
      </div>

    <div>
      <label for="hours">Hours per month you are willing to serve:</label>
      <input type="number" id="hours" name="hours" min="0" required value="<%= volunteer.vol_hours_per_month %>">
    </div>

    <div>
      <button type="submit">Submit</button>
    </div>
  </div>
  </form>
  
  <footer id='footer' class="footer">
    <p>&copy; 2024 Turtle Shelter Project | All rights reserved | 
        <a href="mailto:turtleshelterproject@gmailcom">turtleshelterproject@gmail.com</a> |
        <a href="tel:(801) 872-3190">(801) 872-3190</a>
    </p>
</footer>
</body>
</html>
